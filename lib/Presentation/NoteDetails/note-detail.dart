import 'package:flutter/material.dart';

class ScreenNoteDetails extends StatelessWidget {
  const ScreenNoteDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('note details');
  }
}