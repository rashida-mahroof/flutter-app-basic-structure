import 'package:flutter/material.dart';

class ScreenCreateNote extends StatelessWidget {
  const ScreenCreateNote({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Create note');
  }
}